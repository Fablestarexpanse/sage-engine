# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.env.*

# Node (UIs)
node_modules/
**/node_modules/

# Tools / caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
*.log
logs/

# Runtime game data (generated portraits, room art, etc.)
/data/

# Local LLM data (docker-compose volume)
ollama_data/

# Local assistant / IDE
.claude/

# Live config files (contain credentials — commit *.example.toml instead)
config/database.toml
config/server.toml
config/comfyui.toml
config/llm.toml
config/redis.toml

# OS / editor
.DS_Store
Thumbs.db
.idea/
config/agents_llm.toml
models/

# Local agent tooling (rexyMCP executor config and contract, desloppify state). Not part of the
# engine; each developer keeps their own copy.
/rexymcp.toml
/REXYMCP.md
/.desloppify/
