/target
/graft
*.db
*.db-wal
*.db-shm
.env

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Log fixtures recorded by older engine builds; see the test that reads each one.
!crates/sage-agents/tests/fixtures/*.db

# Browser client: dependencies and build output (embedded into `sage` at build time).
/client/node_modules/
/client/dist/
*.db.lock
*.db.fragments/
