[package]
name = "sage-host"
description = "SAGE: Wasmtime host for code fragments: the plugin seal (WIT imports must match declared capabilities), fuel and memory limits."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
sage-core.workspace = true
serde_json.workspace = true
thiserror.workspace = true
wasmtime = { version = "48", default-features = false, features = ["component-model", "cranelift", "runtime", "std"] }

[dev-dependencies]
sage-build.workspace = true
sage-schema.workspace = true
sage-store.workspace = true
