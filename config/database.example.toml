# Copy to database.toml and fill in your credentials.
# database.toml is gitignored — never commit real passwords.
host = "localhost"
port = 5432
database = "sage"
user = "sage"
password = "CHANGE_ME"
pool_size = 10
