# Copy to server.toml and adjust for your environment.
# server.toml is gitignored — never commit this file.
websocket_port = 8001
max_connections = 100
tick_rate = 0.25

# World package to run, a directory under worlds_dir. Required when worlds_dir holds several.
# "demo" is the four-room world that ships with the engine; "rivermoot" is the full reference world.
world = "demo"
# worlds_dir = "worlds"

# Set to true only for local development. Seeds staff/test and player/test accounts.
dev_mode = false

# DEV-AUTH:BEGIN
# Passwordless logins for loopback clients, needs dev_mode: the player client's dev login box
# (a named character or the character chooser) and the admin console's head-admin dev login.
# Never enable on a networked host. Stripped for release (docs/dev/DEV_AUTH.md).
dev_login = false
# DEV-AUTH:END

# Always true in production. Requires a JWT secret (see below).
admin_auth_required = true

# REQUIRED when admin_auth_required = true.
# Use: SAGE_ADMIN_JWT_SECRET env var (preferred) or set here.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# admin_jwt_secret = "CHANGE_ME"

# Allowed origins for the admin/player UIs. Comma-separated list.
# cors_origins = ["http://localhost:5173", "http://localhost:5174"]
