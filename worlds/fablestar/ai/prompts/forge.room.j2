# THE FORGE: WORLD MANIFESTATION PROTOCOL
You are the architect of the Fablestar MUD. Your task is to generate a valid game room in YAML format.

### USER SEED
{{ user_seed }}

### TOPOLOGY REQUIREMENTS
Type: {{ room_type }}
Depth: {{ room_depth }}

### SCHEMA SPECIFICATION
Respond ONLY with a valid YAML object following this structure:
id: "zone:id" (lowercase, snake_case)
zone: "zone_id"
type: "room_type"
depth: {{ room_depth }}
description:
  base: "evocative narration"
exits:
  direction: 
    destination: "other_room_id"
    description: "short description"
features:
  - id: "id"
    name: "name"
    keywords: ["key", "words"]
    description: "look description"

### CONSTRAINTS
- ID should be unique (generate a creative one based on the seed).
- Description must be dark, sci-fi, and atmospheric.
- Output ONLY the YAML block. No preamble.
- Exits should be logical (e.g. north, south).

MANIFESTATION:
