# SAGE decoupling — decision log

NEXT: M2 complete (PRs #7-#12, repository Fablestarexpanse/sage-engine). 2026-09-14 license and hygiene pass:
plugins/ licensed FSL, NOTICE rewritten and checked in CI (scripts/notice_check.py), database defaults are sage.
Open for the owner: create the private Fablestar repository (plan: docs/sage/FABLESTAR_PRIVATE_REPO_PLAN.md);
review docs/sage/ONBOARDING_PLAN.md before any scaffold work; Fablestar content has three dangling exits;
rotate the Postgres password that docker-compose.yml used to ship as a default (still in git history).
Known history: commit 9a831ea does not boot (be48da3 completes it). Review page: https://claude.ai/code/artifact/2d488113-1ca9-4f45-b993-ac0253e0670c

Append-only. One entry per ruling or decision, newest last. Owner rulings are binding; architect
decisions are proposals until the Phase 1 review approves them.

| Date | Who | Decision | Why |
|---|---|---|---|
| 2026-09-13 | Owner | Brief received (`docs/sage/BRIEF.md`). Scope of this session: Phase −1, Phase 0, Phase 1, then stop. | Brief §5. |
| 2026-09-13 | Owner | "The standalone map tool" in the brief means the **WorldForge Tauri app** (`worldforge/`). | Brief said map tool built / WorldForge unbuilt; the repo's WorldForge is built and its README calls it the map tool. |
| 2026-09-13 | Owner | Phase 0/1 deliverables live in **`docs/sage/`** in the repo, plus a private Artifact copy. | Versioned with the code. |
| 2026-09-13 | Owner | **Agents (computer-controlled players) become a whole plugin.** The engine knows nothing about them. | Chosen over "engine runtime + world data". Second world can run without agents. |
| 2026-09-13 | Architect | Brief's four Phase −1 bugs are already fixed in the tree; replaced with three audit-found breakages (admin builder drops `floors`, tick errors swallowed, removed commands never unregistered). | Verified by reading code; see PHASE0_AUDIT.md §Phase −1. |
| 2026-09-13 | Owner | **One database per world** (contracts B.8 accepted). | Only way to keep "no `world_id` columns" and "switch worlds = config + restart" together. |
| 2026-09-13 | Owner | **Glyph, ship, star-system and galaxy surfaces are not needed at this time** — delete them (models, editors in WorldForge and admin-ui, glyph admin tab, mock client panels) when Phase 3/5 reaches them. Recoverable from git if the design later wants them as a Fablestar plugin content type. | No content, no runtime (audit §11.7); brief §8 prefer deleting. |
| 2026-09-13 | Owner | **Rename approved.** Official name: **"SAGE - Synthetic Agent Game Engine"**; Python package `sage` in Phase 2b per contracts F.1. | Answers contracts G.5. |
| 2026-09-13 | Owner | **License: Functional Source License, `FSL-1.1-Apache-2.0`, engine only.** Licensor Brian M. Taylor. Fablestar world content stays proprietary, all rights reserved. Spec: owner's `sage-fsl-license-setup.md` (applied in commit after this entry). No commercial-waiver contact yet (step 7 skipped). | Replaces the undeclared MIT in `pyproject.toml`; answers contracts G.8. |
| 2026-09-13 | Architect | License applied with three deviations from the owner's spec, all forced by the canonical text: (1) the official identifier is `FSL-1.1-ALv2`, not `FSL-1.1-Apache-2.0`; (2) the template has only `${year}` and `${licensor name}` placeholders, so "SAGE" is named in `NOTICE`/`LICENSE-FAQ.md` rather than inside `engine/LICENSE`; (3) year set to 2026 (not yet published — revisit at first publication). `engine/LICENSE` is byte-identical to `https://fsl.software/FSL-1.1-ALv2.template.md` (sha256 36b60822…56177) except the copyright line. Because `engine/` and `worlds/` don't exist yet, `NOTICE` maps today's paths to each component and excludes Fablestar material still embedded in engine paths. | Owner spec step 2 said confirm layout first. |
| 2026-09-13 | Architect | **Flag for legal review, not resolved:** the owner's FAQ promise "you can build and sell your own game/world on SAGE" is not stated in the FSL text, and Competing Use clause 2 ("substitutes for any other product or service we offer using the Software") arguably covers a commercial MUD because Fablestar Expanse exists. `LICENSE-FAQ.md` states the intent and marks it pending legal review. Options for the owner: accept the ambiguity, add an explicit additional permission alongside the license, or get counsel. Also: don't publish `engine/` until Phase 3 has extracted Fablestar content from engine paths. | Brief §8: flag, don't guess. |
| 2026-09-13 | Owner | G.1: **No separate WorldForge spec yet.** WorldForge's roadmap comes from the contracts (D.E). | — |
| 2026-09-13 | Owner → Architect | G.4 delegated ("whatever makes it more modular and editable for a user"). **Decision: every game mechanic is a first-party plugin, including ambient, effects, combat and equipment.** The engine keeps only runtime infrastructure and the universal verbs (look, move, say/emote/tell, who, help, inventory/take/drop/examine, quit). Plugins may export services and define resolver slots for other plugins (catalog #13). | Maximises what a world author can switch on/off or replace from `world.toml`/Nexus without touching the engine. |
| 2026-09-13 | Owner | G.6: **Retire the admin-ui World Builder**; WorldForge is the only structural editor. | Confirms D.E. |
| 2026-09-13 | Owner → Architect | G.7 delegated. **Decision: two test tiers.** Hermetic unit tests (fakes) stay the default and must pass with no services. A required live tier (real Postgres + Redis, in CI and via a pytest marker locally) covers migrations, persistence, plugin install/uninstall and world smoke tests. "Never mock the database" now applies to that tier's concerns: migrations and persistence are never tested against fakes. | Keeps the fast loop; puts real-DB coverage where mocks hid migration failures before. |
| 2026-09-13 | Owner | G.9: **SAGE has no set genre** — it builds whatever world a user imagines. The second reference world is a test harness only; architect picks its setting to be maximally unlike Fablestar. **Decision: low-fantasy river town, attributes Might/Wits/Nerve, currency silver, `levels` progression, no agents, no AI images.** | Brief §6 still requires a deliberately different world. |
| 2026-09-13 | Owner → Architect | G.10 delegated ("best long-term"). **Decision: declarative panels are the v1 contract, with a reserved `kind = "module"` for plugin-shipped client code.** The manifest schema accepts the key and boot rejects it with "not supported in this engine version", so adding client modules later is additive, not breaking. | Long-term extensibility without building a client plugin pipeline for two worlds. |
| 2026-09-13 | Owner | G.2–G.10 answered; **"looks good" = PHASE1_CONTRACTS.md approved** with the amendments above. Hard stop lifted. | Brief §5. |
| 2026-09-13 | Architect | Stage 2b renames done as four commits (package → `sage`; Python tree → `engine/`; clients/tools → `engine/clients`, `engine/tools`; identity strings). Compatibility for one release: `FABLESTAR_*` env vars (deprecation warning), `fablestar` console script, and a browser-storage migration from `fablestar_*` to `sage_*` keys. The ratchet baseline was raised only for those shims (`engine/clients/*/src/storageMigration.js`, `engine/tests/test_config_env.py`, 22 hits) while 111 hits dropped elsewhere; delete the shims and their baseline entries next release. World branding in the player client (title, "FABLESTAR" header, "Connected to Fablestar Expanse") stays for Phase 3 lexicon/theme work. | Contracts F.1; brief §8 incremental, playable at every commit. |
| 2026-09-13 | Architect | WorldForge Tauri identifier changed `com.fablestar.worldforge` → `com.sage.worldforge` (productName "SAGE WorldForge"). One-way for local app data: WorldForge forgets its saved content folder once; exe auto-detect (now 8 levels) finds the repo, else pick the folder again. | Engine tool identity; flagged in F.1. |
| 2026-09-13 | Architect | Second reference world named **Rivermoot** (slug `rivermoot`); stat keys `mgt`/`wts`/`nrv` (not common English words, so the invariant ratchet can auto-deny them in engine code); world-private `levels` plugin. License of `worlds/rivermoot/` left undecided (NOTICE) — owner question. | G.9 delegated; brief §6. |
| 2026-09-13 | Architect | **Wallet before factions (phase-3 plan 3.2).** In-world money is an engine service over the world's `currencies.yaml` (first currency = primary, name via lexicon `currency.<key>.name`, overdraft refused). `server.game_currency_display_name` / `starting_digi_balance` config deleted. Balance still mirrored to `characters.digi_balance` until the schema step. | Missions, shop and lodging all pay or charge; extracting any of them first would bake Fablestar's currency key into a plugin. |
| 2026-09-13 | Architect | **Plugins may edit a whole character, within a seal (phase-3 3.3).** `api.state.edit(player_id)` loads the full stats blob for work that spans a plugin's own blocks and engine services (wallet, counters); on exit any other changed top-level key raises `PluginError` and nothing is saved. Chosen over giving plugins raw `set_player_stats`. | Mission payout moves money, bumps counters and clears the plugin's own block in one step; per-block `state.set` couldn't express it without losing the ownership check. |
| 2026-09-13 | Architect | **Shop keepers are named by character name, not agent persona id.** Room YAML `owner:` changed for Fablestar's two keeper shops; keeper takings use the engine wallet's `pay_later`/`bank_pending` (`wallet_pending:<name>`). | A shop plugin that resolved persona ids would depend on agents; a character name works for agents and human keepers alike, and every lookup the admin view needs (location, wallet, inventory) is generic. |
| 2026-09-13 | Owner | **Agent durable state moves to a plugin-owned table `plg_agents_state`** (copy existing `agent_state` rows first, drop the engine table in a later commit; uninstalling the plugin drops it). Chosen over engine accountless characters and over pausing agents. One-way door acknowledged. | Keeps the engine ignorant of agents; plugin tables roll back on uninstall (locked decision 8). |
| 2026-09-13 | Architect | **Conduit code moves from the engine (FSL) into `worlds/fablestar/plugins/conduit` (proprietary per NOTICE).** It is Fablestar-only progression; the engine keeps only slots and defaults. | Brief: world mechanics are world packages; the licence line follows the path split already in NOTICE. |
| 2026-09-14 | Owner → Architect | Delegated ("make best choice"). **Rivermoot is licensed with the engine, FSL-1.1-ALv2** (`worlds/rivermoot/LICENSE`, byte-identical to `engine/LICENSE`). | It is the engine's example world: it ships with SAGE, carries no proprietary setting, and world authors will copy it as a template. Matching the engine keeps one set of terms for everything SAGE ships; the grant can later be widened (e.g. Apache-2.0 for templates) but a permissive grant could never be narrowed, so the narrower of the two options is the reversible one. |
| 2026-09-14 | Owner → Architect | Delegated. **Stage branches go up as stacked PRs, not merged into one:** `sage/stage-2b` → base `sage/phase-0-1` (PR #7), `sage/stage-2c` → base `sage/stage-2b`, `sage/phase-3` → base `sage/stage-2c` as a draft until Phase 3 ends. Each PR shows only its own stage; merge in order, and GitHub retargets the next PR when its base merges. | The branches are strictly linear (each contains the previous). One big PR would be unreviewable (191 commits); PRs all based on main would repeat earlier stages in every diff. |
| 2026-09-14 | Architect | **Ratchet exception, 1 hit:** `engine/alembic/versions/p9q0r1s2t3u4_wallet_into_stats.py` names the legacy wallet column once (`LEGACY_COLUMN`), because a migration that moves data out of a column has to say which column. The same commit removed 88 hits elsewhere (633 → 545). Later migrations that rename or drop legacy world-named columns get the same one-constant treatment and a line here. | A migration's SQL is schema history, not runtime engine code; the alternative (discovering the column indirectly) would hide the name without removing it. |
| 2026-09-14 | Architect | **Ratchet exception, 1 hit:** `engine/alembic/versions/q0r1s2t3u4v5_account_ai_credits.py` names the old AI-credit column once (same rule as p9q0r1s2t3u4). `sage.core.config._COMFYUI_RENAMED_KEYS` keeps the two old comfyui.toml key names for one release (deprecation shim, like the `FABLESTAR_` env aliases); delete it next release. | Renames need the old name; the commit removed 216 hits elsewhere (545 → 328). |
| 2026-09-14 | Architect | **3.14d drops applied** (contracts D.D one-way doors, approved at Phase 1 review; owner "make best choice ... and continue"). Made recoverable: the drop migration refuses while a standing value is left behind, its downgrade re-creates the columns so earlier downgrades copy values back from stats, and the dev DB was dumped before each schema step (scratchpad `devdb_before_314*.sql`). Ratchet exception, 1 hit: `r1s2t3u4v5w6` names the dropped wallet column once. | Every value already lived in stats or plugin tables; keeping the columns would leave two sources of truth. |
| 2026-09-14 | Architect | **`chargen.options` carries a `kind`.** The player client renders one kind, `skill_points` (title, budget, per-leaf cap, leaf catalog; submitted as `{"proficiencies": {leaf: level}}`), and skips the choices step for anything else, including the engine default `{}`. Conduit declares it with a lexicon title. `ensure_test_user.py` now creates accounts only: characters come from the normal create path so the world's start room, wallet and chargen seeds apply. | Before, a world without chargen options (Rivermoot) showed a skills step with "response was unexpected", and the script wrote a `test_zone:entrance` room and the dropped `digi_balance` column (broken since 3.14). One renderable kind is the two-world ceiling; a second kind waits for a world that needs it. |
| 2026-09-14 | Architect | **Plugin admin pages are keyed by admin tool (3.13d, narrowed).** admin-ui keeps one page per plugin admin tool (`skills`, `agents`, `shops`) and shows it only when `GET /admin/plugin-pages` reports a plugin of the running world mounting that tool, pointing it at that plugin's `/plugins/<id>/admin` base. A page's data contract belongs to the tool (skills: GET/PUT `<base>/catalog` with `weight_keys`), not to a plugin name. No declarative admin panel kinds yet. | The three pages were always visible and hardcoded plugin URLs, so Rivermoot's admin showed broken pages and the engine client named Conduit. Keying by the existing tool id needed no new manifest touch and no plugin-shipped JS; declarative admin panels wait for a plugin whose admin needs are form-shaped. |
| 2026-09-14 | Architect | **Engine attribute point-buy from `stats.yaml`.** A world with `chargen.attribute_points` gets engine `chargen.*` defaults of kind `attribute_points`: the budget caps the sum of all attribute values, each value stays within its min..max, and unspecified attributes keep their default. A plugin that provides the chargen slots still wins. | Rivermoot declared `attribute_points: 8` and nothing used it. Point-buy over a declared stat schema is world data, not a mechanic, so it needs no plugin. A sum cap reads the same in both worlds' numbers (Fablestar: 5 x 13 = 65). |
| 2026-09-14 | Architect | **Engine-owned stats keys are sealed per world, not per editing plugin.** `api.state.edit` accepts a change to a top-level engine key (vitals) when any loaded plugin declares it in `stats_keys`, not only the plugin holding the edit. | Event subscribers write through the publisher's blob by design (`EntityKilled.stats`). With a per-plugin check, Rivermoot's level-up (`levels` declares `max_hp`) aborted combat's save and lost the kill. Tracking which callback wrote which key would need a proxy on every blob; the per-world declaration still refuses keys nobody declared. |
| 2026-09-14 | Architect | **Rivermoot narrates in text; the no-AI proof moves to a test.** Rivermoot ships room and combat narration templates, but no image templates (G.9: no AI images). The invariant "some world runs without AI" becomes two checks: some world ships no image slots, and a world with no templates has every engine slot disabled. CI's servers also run with the LLM off. | 4.6 gave Rivermoot its own voice to prove the style and slot seams in a second world. That broke a test written when Rivermoot had no AI at all (CI failure on 0ac91bf, missed locally because only the Rivermoot tests were run before that commit). |
| 2026-09-14 | Architect | **WorldForge's Nexus write-through backend (D.E) is deferred.** WorldForge keeps editing world packages on disk. When someone needs to edit a remote server, the backend is a whole-file write endpoint: a content hash refuses stale writes, the staff tool and `may_write_zone` gate it, and `sage.world.lint` validates before the file is replaced. It is not the per-room builder routes removed in 3.18a. | No one edits a remote world today, and both reference worlds live as packages in the repo. Building it means staff sign-in for a desktop app and multi-writer conflict handling for an absent user (two-world ceiling). Deferring is reversible; the shape above keeps it from coming back as a second editor API. |
| 2026-09-14 | Owner → Architect | Owner: "a way to temp skip passwords while in development ... then remove before final release". **Decision: passwordless dev logins behind the existing `dev_mode` + `dev_login` flags, marked for mechanical removal.** Player dev login gains the character chooser (empty name); the admin console gains a head-admin dev login (`dev-staff`, no usable password). All routes live in one module mounted only when both flags are on, and accept a proxied request only when every forwarded client address is loopback; the player and admin Vite dev proxies now send `X-Forwarded-For` (`xfwd`). Every piece sits in a `DEV-AUTH` file or block; `scripts/release_check.py` lists them (exit 1) or strips them (`--strip`), then scans live code for leftover dev-login references. The live world smoke tests register real accounts, so a stripped tree still passes. | Chosen over a config switch that disables password checks: a switch can be left on in a release, while stripped code cannot run. Chosen over a separate dev build: one tree keeps dev and release in step, and the strip is verifiable by running the tests on the stripped copy. The forwarded-address rule closes a hole the earlier loopback-only dev login had: both dev clients reach Nexus through the Vite proxy, and the documented `--host` flag let any LAN browser relay through it as 127.0.0.1. |
| 2026-09-14 | Owner | **First-party plugins (`plugins/`) are licensed FSL-1.1-ALv2**, the same as the engine (`plugins/LICENSE`, byte-identical to `engine/LICENSE`; the canonical template fetched from fsl.software matches the recorded sha256 36b60822…56177). No plugin has different terms. | Before, `plugins/` had no license at all, so a fork got the engine but no grant to combat, shops or equipment. Chosen over Apache-2.0 now and over a per-plugin split; none of the 14 plugins contains Fablestar setting terms. |
| 2026-09-14 | Owner | **Fablestar Expanse moves to a private repository** (chosen over leaving it readable-but-unlicensed and over moving only `docs/design/`). Not done in this pass: it needs worlds outside `worlds_dir` and a neutral fixture world for the 18 plugin test files that read Fablestar content. Plan: `docs/sage/FABLESTAR_PRIVATE_REPO_PLAN.md`. The owner creates the repository. | Makes the proprietary boundary physical. The content is already in public history; rewriting history is a separate decision and is not part of the plan. |
| 2026-09-14 | Architect | **NOTICE lists every top-level path and world package, and CI enforces it** (`scripts/notice_check.py`, in the licenses job). Repository support files (`scripts/`, `config/`, `.github/`, `docs/` except Fablestar material, root files) are named under the engine's FSL terms, which NOTICE previously stated only for `scripts/`. The "Transition period" section is gone: the four engine paths it named no longer exist, and the engine holds no Fablestar setting material beyond legacy identifiers and test fixture names, which the Fablestar-names clause still covers. | A path NOTICE does not name has no stated terms; a path it names that is gone is a stale grant. A check makes both impossible to reintroduce, like the invariant ratchet. |
| 2026-09-14 | Architect | **Generic defaults, breaking:** `DatabaseConfig` defaults to database and user `sage` (was `fablestar`), matching `docker-compose.yml`, which now reads `POSTGRES_DB`/`POSTGRES_USER` from `.env` with `sage` defaults. The `FABLESTAR_` environment prefix and the `fablestar` console script are removed in 0.3.0. Local tooling (`rexymcp.toml`, `REXYMCP.md`, `.desloppify/`) is untracked and gitignored; `docs/architecture.md` is the canonical architecture and `CLAUDE.md` holds only agent conventions. Ratchet 43 to 39. | The rexyMCP project id is a local telemetry identifier, not a credential, so it is untracked rather than rotated. |
| 2026-09-14 | Architect | **Onboarding plan proposed, not approved** (`docs/sage/ONBOARDING_PLAN.md`). Target: `git clone` to walking a self-made three-room world in under 15 minutes, 6 commands, one terminal, no hand edits before first play. Baseline measured from a fresh clone: 18 commands, 5 hand edits, 3 terminals, and registration returns 500 until an undocumented JWT secret is set. Proposed: `sage quickstart` over Docker Compose (SQLite dev mode rejected), a purpose-built minimal world template for `sage world new`, `sage plugin new`/`check` generating manifests from one feature table, filesystem plugin distribution with no `plugin install` yet. No scaffold work before owner review. | Task Part B; the measurement found the JWT-secret failure every newcomer hits. |
| 2026-09-14 | Owner | **Onboarding is two stages: engine first, world building second.** "Starting the world should not include building the map, that's part of the tools (the map builder); get the player set up with the engine first, world building second; it can come with a default 4-room start as a demo." Plan revised: Stage 1 target is `git clone` to walking a bundled four-room demo world (`worlds/demo/`, the quickstart default) in under 10 minutes and 5 commands; `sage world new` makes a package with one start room and no map, and the map is drawn in WorldForge (which gets a New world action). The demo world's license and NOTICE entry are an owner decision still open. | Keeps map authoring in the editor instead of a second, scaffold-shaped way to make rooms. |
| 2026-09-14 | Owner | **Onboarding plan approved** ("yes continue"), including FSL-1.1-ALv2 for the demo world. Done: the server refuses to start without a JWT secret while admin auth is on (it used to start healthy and return 500 on the first registration), and `worlds/demo/` ships four rooms around a hub with no plugins, built with the WorldForge map tools; `config/server.example.toml` now runs it. Demo stat keys `bod`/`mnd`/`spr` and currency `crowns` were chosen because they appear nowhere in engine or plugin code, since the ratchet denies every world's stat and currency keys in engine code. | Stage 1 items 1 and 2 of `docs/sage/ONBOARDING_PLAN.md`. |
| 2026-09-14 | Architect | **`sage quickstart` (Stage 1 item 3).** Writes only missing config (`.env`, `server.toml` with world, JWT secret, `dev_mode`, and `dev_login` inside DEV-AUTH markers; `database.toml` with `sage_<world>`), reusing an existing password from either `.env` or `database.toml` so the two cannot disagree. A world other than the configured one runs in `sage_<world>`, so switching worlds never mixes data. Migrations run as a subprocess (`sage db upgrade`): run in-process, Alembic's logging config doubled every later server log line. Postgres/Redis readiness is a real connection, not a container health flag. Player client still starts separately until item 4. | One database per world (owner ruling) and never overwriting a developer's config. |
| 2026-09-14 | Architect | **Nexus serves the built player client from its 404 fallback, not a `/` mount** (Stage 1 item 4). Plugin routers are included after the app is built, and a Starlette mount at `/` matches every path, so it would shadow them; the fallback only answers GET/HEAD 404s for `/`, `/assets/*` and root-level files, and only with files inside the build directory. Those paths are public in the auth middleware; no existing API route has such a path. The client build's API base is now its own origin (was `127.0.0.1:8001`); `VITE_NEXUS_URL` covers builds hosted elsewhere. | One terminal for a newcomer without a second web server, and without changing any API route's auth. |
| 2026-09-14 | Architect | **Stage 1 tutorial written and walked through, not yet timed with a real reader** (item 5). Walking it in a fresh clone changed the text in three places: demo room descriptions are now one YAML line each (wrapped plain scalars were hard to edit safely); a new section covers "You are in the void.", which is what a player sees when a room file does not parse (an unquoted `: ` in the text is the likely cause, and quoting fixes it without a restart); PowerShell's script execution policy blocking `Activate.ps1` is noted. The "under 10 minutes" target stays unconfirmed until a first-time reader is timed. | The plan requires the tutorial be verified by following it, and the first-time-reader timing cannot be done by its author. |
| 2026-09-14 | Owner → Architect | Admin console audit (artifact 90387dba…). Owner: "continue with fixes". **Steps 1 and 3 done:** `GET /admin/world` became the console's source for the running world, plugins, AI slots and online counts, with humans and agents counted apart. A plugin's "world plugin" or "shared" label comes from where it lives, not from the manifest's `first_party` flag, because Fablestar's conduit and morality declare `first_party`. The audit said Forge could write void rooms; wrong, `validate_room_yaml_text` already refused bad YAML and ids, so the added check is world-level (`sage.world.lint.lint_room_candidate`, run on a temporary copy of the content). | Nothing on screen should be false before new admin pages are built on it. |
| 2026-09-14 | Architect | **Live Activity carries WARNING and above** (`AdminLogBroadcastHandler`, installed on the root logger while Nexus serves). INFO would flood the feed with access logs and agent chatter. A record from the Nexus module itself is never forwarded, so a failing socket cannot feed back into it. | A console feed nobody could read was the original bug; errors are what an operator needs to see live. |
| 2026-09-14 | Architect | **Admin audit step 2: World & plugins page** (nav id `world`, visible with the world, server or dashboard tool). `GET /admin/world/check` runs `lint_world` in a thread and checks pending migration heads. The migration check reports "unknown" with the error when the database is unreachable, so the lint result still shows. The page shows the running world only; it does not edit it (WorldForge edits content, `world.toml` stays a file). The legacy `fs-admin-nav` mapping of `world` to Content Library was dropped, because the id now names this page. | The audit's biggest gap: nothing in the console showed which world or plugins were running or whether content validated. |
| 2026-09-14 | Architect | **Admin audit step 5: room detail, template editor, staff editing.** Rooms stay read-only in the console: their detail links to other rooms and shows live state and findings, but edits belong to WorldForge (G.6). Entity and item templates are edited as YAML in the console, because they are flat files WorldForge's map builder does not cover; saves go through `validate_template_yaml_text` (parse, model, id). A long-standing bug surfaced: `_register_template_routes` used `Annotated[AdminContext, Depends(require_tool(tool))]` with `tool` a local, which postponed annotations cannot resolve, so every template YAML call returned 422. The dependency is now a default value. | The audit found these backend routes existed but the UI never called them; calling them showed they had never worked. |
| 2026-09-14 | Architect | **Admin audit step 6: character tools, suspension and an audit log.** Staff edits to a character write Postgres and, when the character has live state in Redis, Redis too. Redis state survives logout and the persistence flush copies it over Postgres every ~60 s, so a Postgres-only edit (the account editor's character save until now) silently reverted; a live test proves the revert without the fix. The audit log is one table (`admin_audit_log`) fed two ways: character and suspension routes write their own rows with before/after detail, and an ASGI middleware records every other successful write (POST/PUT/PATCH/DELETE, 2xx) as route template plus scrubbed body. Logins, generation and test calls are not recorded; secret-named keys at any depth are stored as "(changed)"; a failed audit write is logged and never fails the request. Suspension is two account columns and is checked after the password, so it is not revealed to wrong guesses. | The audit found no way to fix a stuck or misbehaving player and no record of who changed what. A middleware covers every existing and future write route without touching each one; explicit rows keep the detail that matters for character changes. |
| 2026-09-14 | Owner | **Back office audit rulings** (artifact 659aa1d7…, 8 proposed steps). (1) Staff work happens **both** in the Nexus console and through in-game staff commands. (2) Login addresses are stored **only when an operator turns it on**, with safeguards so a deployment can meet its own country's rules (off by default, a retention period, purge, and a disclosure to players). (3) Nexus no longer creates rooms or zones: **WorldForge is the only room editor**. (4) The GM crown becomes **real staff power and authority** in-game, tied to a staff account, instead of a badge. | Owner answers to the audit's four open questions. |
| 2026-09-14 | Architect | **Back office step 1: menus by staff job.** The console places each plugin admin page (keyed by its admin tool) in a job group in its own nav table: skills under World, shops under Economy, agents under NPCs. This replaces the audit's proposed `admin_section` manifest touch. The console already ships a page for each known admin tool, and a plugin cannot ship its own page (no plugin JavaScript), so a manifest field would only repeat what the page table already says. Add the field when a plugin's admin page is generated from declarations. Role presets are server data (`TOOL_PRESETS`) so a test can refuse a preset that names a tool the server no longer checks. | Audit finding: menus were grouped by implementation ("Plugins", "AI"). Owner ruling (3) removed the Nexus room writer. |
| 2026-09-14 | Architect | **Back office step 2: a room's player set means connected.** Only a connected session (a player or an agent) belongs in `room:<id>:players`. A staff move of an offline character writes the location key only (`RedisState.set_player_location_offline`), and login adds the character to the set as before. Names found in a set without a session are shown as "left behind" and can be cleared from Live world. Admin views read Redis through `scan_sets` and `scan_states`, which scan the keyspace. List routes cap their rows at 500 and always count the full total. | Step 6's move tool wrote an offline Dev Tester into the clinic's player set, and the live-world count then disagreed with the Dashboard. Treating the set as "who is here now" matches what `look` and `who` already assume. Crashes can still leave names behind, hence the clear action. |
| 2026-09-14 | Architect | **Back office step 3: scale without a content database.** Templates stay files. The console reads them through a parse cache keyed by each file's mtime and size, using libyaml's safe loader when it is installed. Search, sort and paging happen on the server over that cache. Table columns come from the template model plus the world's content extensions: an object or list field shows its entry count, and each creature `stats` key is its own column. Accounts and characters page in SQL, and account character counts come from one grouped subquery. Every record the console opens has a hash address. | At 5,000 item templates, listing took 0.64 s on the first call and 0.16 s after it. The alternatives were a content table in Postgres or a search index. Both would give content a second source of truth next to the files that WorldForge and git own. Revisit if a world passes about 50,000 template files. |
| 2026-09-14 | Architect | **Back office step 4: references are found by id, not by a list of fields.** To find what names a record, the console walks every room, item and creature file. Any top-level field whose value, or a key nested inside it, equals the record's id counts, except the record's own file. Global search runs each group only for staff with that group's tool, and returns the console address of every result. | A per-plugin list of reference fields (shop stock, recipes, loot) would put plugin knowledge in engine admin code, and it would go stale whenever a plugin added a field. Matching by id finds all of them on Fablestar with no list. For example, `charge_cell` is named in 2 shops, 2 scraps, 1 ammo and 1 loot. The cost is false positives when an id equals some other value. Ids are namespaced slugs, and each hit shows its field, so a false positive is visible. |
| 2026-09-14 | Architect | **Back office step 5: snapshots before staff changes, not on a schedule.** `character_snapshots` stores room, stats and inventory. A snapshot is taken after a change is validated and just before it is written, so a refused change leaves none. Each character keeps its newest 50 snapshots. A restore writes the live state as well (the flush rule from admin audit step 6), after first snapshotting the current state. The staff sheet draws the plugins' own player panels from `snapshot_contributors`; the console has no per-world code. A stat label with no lexicon line shows its key instead of `[key]`. | The audit asked for snapshots before staff edits and daily. The daily snapshot is left out: it would copy every character's blob each day to guard against player-side loss, which no one has reported. Adding it later reuses the same table. Found while testing: Fablestar's lexicon has no `stat.*.name` or `vital.hp.name` lines, so those labels are missing in any client that shows them. The Conduit panel's attributes (10) differ from the top-level stat keys (13) for Dev Tester. Both are Fablestar content questions for the owner. |
| 2026-09-14 | Owner → Architect | **Back office step 6: moderation, with sign-in addresses as an operator option** (owner: "yes but with safety in place to meet any regulations, make it an option since each country is different"). `record_login_addresses` is off by default. When it is off, a sign-in row keeps only the time and method. When it is on, `/play/world` reports it and both sign-in screens say so, including how long history is kept. Sign-in rows older than `login_history_days` are deleted by an hourly tick job. Staff can erase addresses for one account (a player's request) or for everyone. Bans work whether or not addresses are recorded, because they are checked at request time and store nothing about players. The address used is the connection peer, and an IPv4 address carried in IPv6 is read as IPv4. Forwarded headers are ignored because any client can set them, so a deployment behind a reverse proxy has to pass the real client address through at the network level. Mutes are read from the session, which is set at play login and updated when staff mute, so chat commands need no database read. Reports come from one engine command with lexicon text and a per-character cooldown. Settings live in `config/moderation.toml` (deployment config, not world data). | The legal line differs by jurisdiction, so the engine offers the safer default plus the tools an operator needs: disclosure, retention, and erasure. Found in the real run: the dev client reached Nexus as `::1`, so an IPv4 `127.0.0.1` ban did not match, which is correct; the test was repeated with `::1`. |
| 2026-09-14 | Architect | **Back office step 7: staff feed from events, money from the database, restart that stops cleanly.** The feed subscribes to engine events (`SessionStarted`, `SessionEnded`, `PlayerDied`, `EntityKilled`, and a new `CharacterCreated`). Reports and restarts add to it directly. It keeps the newest 1000 entries in memory and is polled, not pushed. Level-ups are not in the feed: progression belongs to plugins and has no engine event, and adding one per world would be world-specific. Money sums each currency's top-level stats key in SQL across the `characters` table. Agents' wallets live in the agents plugin's own state, so they are not counted. The restart saves characters (`persistence.flush_all`), ends play sessions, stops the tick loop and asks uvicorn to exit (graceful timeout 10 s). The engine does not re-exec itself: a restart policy or service manager starts it again. | Polling every 3 s keeps the feed on the existing REST and auth path. Nothing needed push-level latency. Keeping the feed in memory makes it a live view. It is not a second audit log, and it stores nothing about players. Re-exec from inside Python would leave half-closed sockets and bypass the process manager that owns the service. |
